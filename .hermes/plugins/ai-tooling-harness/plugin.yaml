name: ai-tooling-harness
version: "1.0"
provides_hooks: [post_tool_call]
provides_middleware: [tool_request]
description: ai-tooling harness adapter — commit gate and plugin/docs auto-sync for Hermes Agent.
